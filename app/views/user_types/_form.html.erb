<%= form_for @user_type, :html => { :class => "form-horizontal user_type" } do |f| %>

  <% if @user_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@user_type.errors.count, "error") %> prohibited this user_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @user_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_role, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_role, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_role) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                user_types_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
